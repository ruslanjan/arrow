version: '3'

services:
  db:
    image: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=4naP3rzdw2cn33PA
  redis:
    image: 'redis:latest'
    command: redis-server --requirepass 3mAH9qBS4L826RfP
    ports:
      - 6379:6379
  web:
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - PRODUCTION_ENV=1
      - CELERY_BROKER=redis://:3mAH9qBS4L826RfP@redis:6379
      - CELERY_BACKEND=redis://:3mAH9qBS4L826RfP@redis:6379
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=4naP3rzdw2cn33PA
      - DB_HOST=db
    volumes:
      - /var/www/tau.moe/media:/code/media # media files
      - /var/www/tau.moe/production_static:/code/production_static # media files
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
  celery:
    build: .
    command: celery -A arrow worker -l info -n celery-host-wroker@%h
    environment:
      - PRODUCTION_ENV=1
      - CELERY_BROKER=redis://:3mAH9qBS4L826RfP@redis:6379
      - CELERY_BACKEND=redis://:3mAH9qBS4L826RfP@redis:6379
#    volumes:
#      - .:/code
    depends_on:
      - db
      - redis
  celery-beat:
    build: .
    command: celery -A arrow beat -l info
    environment:
      - PRODUCTION_ENV=1
      - CELERY_BROKER=redis://:3mAH9qBS4L826RfP@redis:6379
      - CELERY_BACKEND=redis://:3mAH9qBS4L826RfP@redis:6379
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=4naP3rzdw2cn33PA
      - DB_HOST=db
    volumes:
      - .:/code
    depends_on:
      - db
      - redis
  celery-judge:
    build:
      context: .
      dockerfile: Dockerfile_worker
    command: celery -A arrow worker -l info -Q sandbox_execution --concurrency=1 -n celery-judge-wroker@%h
    privileged: true
    cap_add:
      - ALL
    environment:
      - PRODUCTION_ENV=1
      - CELERY_BROKER=redis://:3mAH9qBS4L826RfP@redis:6379
      - CELERY_BACKEND=redis://:3mAH9qBS4L826RfP@redis:6379
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASSWORD=4naP3rzdw2cn33PA
      - DB_HOST=db
    depends_on:
      - db
      - redis
      - web
      - celery
  flower:
    image: mher/flower
    command: ["flower", "--broker=redis://:3mAH9qBS4L826RfP@redis:6379", "--port=8888"]
    ports:
      - 8888:8888
    depends_on:
      - db
      - redis

volumes:
  db-data: